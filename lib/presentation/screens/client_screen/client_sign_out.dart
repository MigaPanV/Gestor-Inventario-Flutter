import 'package:flutter/material.dart';

class ClientSignOut extends StatelessWidget {
  const ClientSignOut({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pestaña para cerrar sesion'))
    );
  }
}